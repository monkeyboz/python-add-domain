#!/usr/bin/python
import subprocess

class network:
	def network():
		print "testing"
	def change_network_layout(self):
		self.network()
		subprocess.call("sudo ifconfig -a", shell=True)
	def get_network_info():
		subprocess.call("sudo echo testing", shell=True)
	def restart_eth_addapter():
		subprocess.call("sudo ifconfig -a")
	def 
